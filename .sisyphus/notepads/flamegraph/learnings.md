2026-04-08: Added a new route variant by updating the shared Route union first, then wiring back-navigation and app switching to keep discriminated-union handling consistent.
2026-04-08: For the TUI icicle flamegraph, grouping spans by depth and then flattening rows top-to-bottom gives simple j/k reading-order navigation, while h/l can jump within the current row via row start offsets.
2026-04-08: Flamegraph bars need per-column clipping rather than naive sibling rendering so overlapping spans at the same depth still produce a single terminal row with visible tails and a minimum width of one character.
2026-04-08 (Task 3): Web flamegraph in src/web.ts uses a `collectByDepth()` helper that recursively walks `trace.tree` (array of WebSpan with `.children[]`) and fills a `rows` array indexed by depth. renderFlamegraph() then builds one `.fg-row` div per depth level with absolutely-positioned `.fg-bar` divs. The `detailTab` module-level variable tracks which tab is active and is reset to "waterfall" in `openTrace()`. Tab switching uses `style.display = ""` to restore (not "block") so the `.waterfall-container` uses its natural flexbox display. The embedded JS in web.ts uses semicolons (existing style) — stay consistent. otel-traces.json works as the test file (it's JSONL, one JSON object per line).
2026-04-08 (Task 4): Aggregated mode groups spans by their full stack path (ancestor chain of "svc:name" joined with " > ") using a nested Map<depth, Map<pathKey, {totalDurationMs, span}>>. This correctly merges identical call stacks across different invocations. In TUI, `layoutRows` memo switches between `buildLayout()` and `buildAggregatedLayout()` based on a `mode` signal; `m` key toggles and resets cursor to 0. In web, `buildAggregatedRows()` uses vanilla JS (semicolons, var-style) matching the existing embedded script style; `renderFlamegraph()` now creates DOM elements via `createElement` instead of `innerHTML` for the container, attaching two button event listeners for the Icicle/Aggregated sub-toggle. Aggregated bars are sorted alphabetically by the leaf segment (the last ` > svc:name` component of the full path key). QA confirmed: Icicle mode produces 3571 bars for a 3571-span trace; Aggregated produces 1428 (correctly merged). The `buildAggregatedRows` function is defined before `renderFlamegraph` so it is available in scope when called.
